#include <stdio.h>

/**
 * main - Entry point
 *
 * print - single digit numbers from 0 t0 9
 *
 * Return: 0 (success)
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
		putchar('i');
	putchar('\n');
	return (0);
}
